[project]
name = "llm-terminal-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.9.2",
    "openai>=1.83.0",
    "psutil>=7.0.0",
]
